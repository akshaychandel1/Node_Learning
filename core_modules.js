const { log } = require('console');
const fs = require('fs');   // importing of core module
const os = require('os');

// fs.writeFileSync('dummy.txt',"Learning modules in node js");  // using of core module

console.log(os.platform());
console.log(os.hostname());
